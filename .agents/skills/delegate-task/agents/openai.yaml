interface:
  display_name: "Delegate Task"
  short_description: "Use OpenSpace to coordinate complex multi-step work"

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "openspace"
      description: "OpenSpace MCP server"
